import { FieldCustomDirective } from './field-custom.directive';

describe('FieldCustomDirective', () => {
  it('should create an instance', () => {
    /*const directive = new FieldCustomDirective();
    expect(directive).toBeTruthy();*/
  });
});
