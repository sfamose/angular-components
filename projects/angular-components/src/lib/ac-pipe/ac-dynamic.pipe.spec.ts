import { AcDynamicPipe } from './ac-dynamic.pipe';

describe('AcDynamicPipe', () => {
  it('create an instance', () => {
/*    const pipe = new AcDynamicPipe();
    expect(pipe).toBeTruthy();*/
  });
});
