import { FilterFieldDirective } from './filter-field.directive';

describe('FilterFieldDirectiveDirective', () => {
  it('should create an instance', () => {
    // const directive = new FilterFieldDirective();
    // expect(directive).toBeTruthy();
  });
});
